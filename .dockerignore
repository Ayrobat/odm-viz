# Ignore everything by default, then whitelist required files
*

# Allow the Dockerfile itself
!Dockerfile

# Allow only the C++ sources needed by the build
!packages/wasm/decoder.cpp
!packages/wasm/LAStools/**
